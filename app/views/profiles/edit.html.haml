.row
  .col-md-4.col-sm-6
    = render "settings", user: @user
    
  .col-md-6
    .panel-container
      %h2 Edit profile
      #edit-profile
        = render "form", user: @user, profile: @profile

  .col-md-2
    .panel-container
      #profile-status
        = render "status", user: @user